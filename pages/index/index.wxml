<!--index.wxml-->
<view class="container">
    <view class="header">EP小工具</view>
    <view class="list_box">
        <navigator class="box" wx:for="{{tools}}" wx:key="id" url="{{item.url}}">{{item.name}}</navigator>
    </view>
</view>
